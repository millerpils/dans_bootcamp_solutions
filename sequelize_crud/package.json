{
  "name": "sequelize_crud",
  "version": "1.0.0",
  "description": "An additional Sequelize CRUD API example that has Restaurants, Menus and Menu Items. Useful to compare\r and contrast with the class based models that the Restaurants example uses.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "sequelize": "^6.6.5",
    "sqlite3": "^5.0.2"
  }
}
